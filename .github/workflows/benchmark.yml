# SPDX-License-Identifier: AGPL-3.0-or-later
# © 1998–2026 Miroslav Šotek. All rights reserved.
# Contact: www.anulum.li | protoscience@anulum.li
name: Performance Benchmarks

on:
  push:
    branches: [main]
    paths:
      - "engine/**"
      - ".github/workflows/benchmark.yml"
  pull_request:
    branches: [main]
    paths:
      - "engine/**"
      - ".github/workflows/benchmark.yml"

permissions:
  contents: write
  deployments: write

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    name: Run Benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable

      - uses: Swatinem/rust-cache@ad397744b0d591a723ab90405b7247fac0e6b8db # v2
        with:
          workspaces: engine

      - name: Build benchmarks
        working-directory: engine
        run: cargo bench --bench bitstream_bench --bench full_bench --no-run

      - name: Run Criterion benchmarks
        working-directory: engine
        run: |
          cargo bench --bench bitstream_bench --bench full_bench \
            | python ../.github/criterion_to_bencher.py \
            | tee ../benchmark_output.txt
          echo "--- benchmark_output.txt ---"
          cat ../benchmark_output.txt

      - name: Clean build artifacts before branch switch
        run: git checkout -- .

      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@a7bc2366eda11037936ea57d811a43b3418d3073 # v1
        if: github.ref == 'refs/heads/main'
        with:
          name: Rust Criterion Benchmark
          tool: cargo
          output-file-path: benchmark_output.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          alert-threshold: "200%"
          comment-on-alert: true
          fail-on-alert: true
          alert-comment-cc-users: "@anulum-maintainers"
