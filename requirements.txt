# Pinned runtime dependencies for SC-NeuroCore v3.10.0
# Canonical source of truth: pyproject.toml [project.dependencies]
# Regenerate with: pip-compile pyproject.toml -o requirements.txt
numpy>=1.22,<3.0
scipy>=1.7,<2.0
