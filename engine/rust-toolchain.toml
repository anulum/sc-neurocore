# SPDX-License-Identifier: AGPL-3.0-or-later
# Commercial license available
# © Concepts 1996–2026 Miroslav Šotek. All rights reserved.
# © Code 2020–2026 Miroslav Šotek. All rights reserved.
# ORCID: 0009-0009-3560-0851
# Contact: www.anulum.li | protoscience@anulum.li
# SC-NeuroCore — Source/config provenance header

[toolchain]
# Exact toolchain pin (not the mutable "stable" channel) so the compiler is
# deterministic and matches the exact `toolchain:` pinned in .github/workflows/ci.yml.
# Bump both together. Residual: rustup still resolves this version from the
# official mutable dist channel without a per-artifact digest — accepted + tracked
# in docs/internal/ci_supply_chain_residuals.md.
channel = "1.97.1"
components = ["rustfmt", "clippy"]
