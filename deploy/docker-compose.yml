# SPDX-License-Identifier: AGPL-3.0-or-later
services:
  sc-neurocore:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command: python -c "import sc_neurocore; print(f'sc-neurocore {sc_neurocore.__version__} ready')"
